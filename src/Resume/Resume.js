import Box from '../Box/Box';

function Resume(props) {
    return (
        <Box anchor="resume" title="Resume">
        <p>
        resume
        </p>
        </Box>
    );
}

export default Resume;
